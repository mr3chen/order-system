spring:
  application:
    name: order
  cloud:
    config:
      name: order  #文件前缀名称
      enabled: true  #enabled 是否从配置中心读取文件
      service-id: config   #配置中心的servieId，即服务名
eureka:
  client:
    service-url:
      defaultZone: http://192.168.114.15:8761/eureka/,http://192.168.114.15:8762/eureka/
      
      
       